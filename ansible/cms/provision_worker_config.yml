# Write cms.conf to all workers

- hosts: laptop_w
  remote_user: ioi
  strategy: free
  gather_facts: no

  vars:
          template: workers

  vars_files:
          - /home/sysadmin/ansible-vars/cms
          - /home/sysadmin/ansible-vars/db
  roles:
          - config
